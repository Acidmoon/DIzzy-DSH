/**
 * Client half entry. Loaded by the dsh web shell through
 * window.__ModuleLoader__.load({ id: 'dsh-subscription-auth', ... }); registers the
 * 「订阅服务」settings-center page. This declaration exists for package
 * exports typing only.
 * @module dsh-subscription-auth/client
 */
export {}
